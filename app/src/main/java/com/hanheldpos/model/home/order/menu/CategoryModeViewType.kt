package com.hanheldpos.model.home.order.menu

enum class MenuModeViewType(val value: Int) {
    Menu(1),
    Empty(6);
    /*
            * 0 : Disable All
            * 1 : Enable Next
            * 2 : Enable Previous
            * 3 : Enable All
            * */
}